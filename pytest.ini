[pytest]
# ==========================================================
# ✅ Clean & Allure-Optimized Pytest Config
# ==========================================================
addopts =
    -v
    -s
    --alluredir=allure-results
    --clean-alluredir
    --disable-warnings
    --tb=short
    --strict-markers
    # Uncomment next lines for local HTML report (optional)
    # --html=reports/report.html
    # --self-contained-html
    # --junitxml=reports/junit.xml

testpaths = tests
pythonpath = .

# ==========================================================
# ✅ Logging
# ==========================================================
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(name)s: %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# ==========================================================
# ✅ Custom Markers
# ==========================================================
markers =
    smoke: quick validation tests
    regression: full regression suite
    critical: core business flow
    slow: performance or heavy tests
    ui: UI-based tests
    api: API-level tests

# ==========================================================
# ✅ Discovery Patterns
# ==========================================================
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

minversion = 6.0
